info:
  name: spaceX
  type: http
  seq: 6

http:
  method: GET
  url: https://api.spacexdata.com/v5/launches/latest
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

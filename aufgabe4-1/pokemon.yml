info:
  name: pokemon
  type: http
  seq: 5

http:
  method: GET
  url: https://pokeapi.co/api/v2/pokemon/snorlax
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
